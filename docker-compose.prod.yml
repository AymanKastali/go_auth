services:
  web:
    image: "${GA_PROJECT_NAME}:latest"
    build:
      context: .
      dockerfile: build/Dockerfile.prod
    restart: always
    ports:
      - "${GA_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy